apiVersion: v1
kind: Service
metadata:
  name: {{ include "turbo-webhook-handler.fullname" . }}
  labels:
    {{- include "turbo-webhook-handler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "turbo-webhook-handler.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
